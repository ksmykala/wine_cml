stages:
  # To understand: why I cannot get data in pipeline?
  # get_data:
  #   cmd: dvc pull data/winequalityN.csv
  train:
    cmd: python train.py
    deps:
      - train.py
      - data/winequalityN.csv
    outs:
      - results/feature_importance.png
      - results/residuals.png
    metrics:
      - results/metrics.txt:
          cache: false